---
:backends:
  - yaml

:hierarchy:
  - "nodes/%{fqdn}"
  - "roles/%{ec2_tag_role}"
  - common

:yaml:
   :datadir: /etc/puppet/environments/%{environment}/hiera
   # If you want it to rely on ec2 tag.
   # :datadir: /etc/puppet/environments/%{ec2_tag_environment}/hiera
